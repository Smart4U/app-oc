{{ field('title', item.title, "Titre de l'article") }}

{{ field('slug', item.slug, "Le slug") }}

{{ field('subtitle', item.subtitle, "Second titre") }}

{{ field('author', item.author, "Auteur de l'article") }}

{{ field('content', item.content, "L'article", {type: 'textarea'}) }}
